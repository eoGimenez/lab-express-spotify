{{#each items}}
  <div id="albumDiv">
    <img class="img" src={{images.0.url}} alt="images" />
    <div class="back">
      <p>{{name}}</p>
      <a href="/tracks/{{this.id}}">View tracks</a>
    </div>
  </div>
{{/each}}