{{#each items}}
    <div id="artistDiv">
        <img class="img" src={{images.0.url}} alt="images"/>
        <div class="back">
        <p>{{name}}</p>
        <a href="/albums/{{id}}">View Albumn</a>
        </div>
    </div>
{{/each}}